# Simple Python Calculator

This is a simple Python script that performs the addition of two numbers.

## How It Works:
1. The script prompts the user to enter two numbers.
2. It adds the two numbers and displays the result.
3. The script uses a function `add_numbers()` to perform the addition.
